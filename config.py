# TODO: This file would load config.json to override its default values

# Default values for configuration
dataDir = './__data/'

convertedDir = './_converted/'
convertedFile = 'store.db'
convertedInfoFile = 'info.pkl'

cvDB = 'CrossVal.db'
extractedDir = './_extracted/'

resultDir = './_results/'

